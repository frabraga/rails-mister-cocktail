<h1>Index</h1>
<ul>
<% @cocktails.each do |c| %>
  <li> <%= link_to c.name, c %> <%= link_to "Delete", cocktail_path(c),
      method: :delete,
      data: { confirm: "Are you sure?" } %> </li>
<% end %>
</ul>

<%= link_to 'New cocktail', new_cocktail_path %>
